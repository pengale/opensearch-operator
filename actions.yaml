# Copyright 2021 Penny Gale
# See LICENSE file for licensing details.
#
# TEMPLATE-TODO: change this example to suit your needs.
# If you don't need actions, you can remove the file entirely.
# It ties in to the example _on_fortune_action handler in src/charm.py
#
# Learn more about actions at: https://juju.is/docs/sdk/actions

injest:
  description: injects a given dataset.
  params:
    fail:
      description: "Unable to import data."
      type: string
      default: ""

creds:
  description: fetch creds for admin dashboard.
  params:
    fail:
      description: "Could not find any admin credentials. Something has gone very wrong."
      type: string
      default: ""
